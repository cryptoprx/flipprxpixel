{
  "name": "flipprx-capacitor",
  "version": "1.1.0",
  "description": "FLIPPRX Pixel Game - Capacitor Android build (self-contained APK)",
  "private": true,
  "scripts": {
    "build": "cd .. && cross-env STATIC_EXPORT=true npx next build && cd capacitor && npx cap sync",
    "open": "npx cap open android",
    "sync": "npx cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^6",
    "@capacitor/core": "^6",
    "@capacitor/cli": "^6",
    "cross-env": "^7"
  }
}
